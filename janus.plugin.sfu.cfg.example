[general]

# The path to a file containing a secret key. If present, this key will be used to validate the signatures
# of JWTs provided when joining rooms in the SFU plugin, and users will not be allowed to join without
# a valid signed JWT.
# auth_key = "foo.key"

# If present, the maximum number of users allowed to join a single room.
max_room_size = 30

# If present, the maximum number of concurrent sessions the server can support. The number of sessions in a room is (number of users)^2
max_ccu = 1000

# Number of threads to run message processing on. If zero, use the # of logical CPUs.
message_threads = 0
